import { FieldFormaterPipe } from './field-formater.pipe';

describe('FieldFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
